<UserControl x:Class="demo.wsclient.RestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" x:Name="_uiName">Rest Method Name</Label>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="1" x:Name="_uiTextBoxData" Margin="2">Data</TextBox>
            <TextBox Grid.Column="2" x:Name="_uiTextBoxCredentials" Margin="2">Credentials</TextBox>
            <Button Grid.Column="0" Click="Button_Click"  Width="80"  Margin="2">Execute</Button>
        </Grid>
    </Grid>
</UserControl>
